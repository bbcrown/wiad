os:
  - windows
  - linux
  - osx
  
language: 
  - r

r:
  - oldrel
  - release
  - devel
  
addons:
  apt:
    packages:
      - gdal-bin
      - libgdal1-dev

r_packages:
  - covr

after_success:
  - Rscript -e 'library(covr); codecov()'