arch: amd64
language: r

jobs:
  include:
   - os: linux
     r:
      - oldrel
      - release
      - devel
   - os: osx
     r:
      - oldrel
      - release


addons:
  apt:
    packages:
      - gdal-bin
      - libgdal1-dev

r_packages:
  - covr

after_success:
  - Rscript -e 'library(covr); codecov()'